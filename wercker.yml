---
box:
  id: dceoy/jupyter:latest
  entrypoint: /bin/bash -c
test:
  steps:
    - script:
        name: Install fract
        code: |
          pip install -U https://github.com/dceoy/oanda-cli/archive/master.tar.gz .
    - script:
        name: Validate the codes using flake8
        code: |
          find fract -name '*.py' | xargs flake8
    - script:
        name: Test base options
        code: |
          fract --version
          fract --help
    - script:
        name: Test commands
        code: |
          fract init --debug
  after-steps:
    - slack-notifier:
        url: $SLACK_URL
        channel: $SLACK_CHANNEL
